#define _CRT_SECURE_NO_WARNINGS 1
/*#include<iostream>
using namespace std;

int main() {
	int a;
	int cot = 0;
	do {
		printf("请输入小于6的正整数：");
		scanf("%d", &a);
	} while (a>=6);
	for (int i = a; i <= a+3;i++) {
		for (int j = a; j <= a + 3; j++) {
			for (int k = a; k <= a + 3; k++) {
				if (i != j && i != k && j != k) {
					printf("%d%d%d  ", i, j, k);
					cot++;
					if (cot % 6 == 0) {
						printf("\n");
					}
				}
			}
		}
	}

	system("pause");
	return 0;
}*/